const prompt = require('prompt-sync')()
let guess=prompt("Password")
while(guess!=="SE11"){
  let guess2=prompt("Try again")
}
let competencies=["if/else","while","for","functions","objects"]
 for(let i=1;i<=5)